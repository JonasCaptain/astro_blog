---
const { date } = Astro.props;
---

<time style="font-style: normal" datetime={date.toLocaleString()}>
	{
		date.toLocaleDateString('zh', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
